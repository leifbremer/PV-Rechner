#ifndef FUNKTIONEN_VON_LEIF
#define FUNKTIONEN_VON_LEIF

void hamburgmonatsmittel(double städte[12], double tage[12], double hamburgtag[12]);		//Funktion errechnet Monatsmittel für Hamburg

void mittlereglobalstrahlung(double städte[12], double neigungsfaktor[13], char namen[10], double peak);		//Funktion für Berechnung der mittleren Globalstrahlung

short getShortMinMax(char text[], short min, short max);

double getDoubleMinMax(char text[], double min, double max);
#endif
